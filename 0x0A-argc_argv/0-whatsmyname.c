#include <stdio.h>
#include <stdlib.h>

/**
  * main - main entry
  * @argc: First parameter
  * @argv: Second parameter
  *
  * Return: exit(EXIT_SUCCESS)
  */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	exit(EXIT_SUCCESS);
}
